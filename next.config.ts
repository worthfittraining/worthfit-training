import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // PWA install support comes from public/manifest.json + meta tags in layout.tsx
  // No additional config needed
}

export default nextConfig
